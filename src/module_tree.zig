pub const ui = struct {
	pub const window = @import("modules/ui/window.zig");
};

pub const graphics = struct {
	pub const gfx = @import("modules/graphics/gfx.zig");
};

pub const math = struct {
	pub const transform = @import("modules/math/transform.zig");
};

